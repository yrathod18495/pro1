# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# npm lockfile (project uses bun.lock)
package-lock.json

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# Python backend (HF Space) — deployed manually, not through this repo's
# GitHub push. Kept out of the repo so it never ends up on GitHub.
/server-files/
